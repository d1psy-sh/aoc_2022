fn main() {
    let input = include_str!("./input.txt");
    let split = input.split("\n\n").collect::<Vec<&str>>();
    // let vec = split.clone().flat_map(|x| x.split("\n")).collect::<Vec<&str>>();
    // let resses = vec.iter().flat_map(|x| x.parse::<usize>()).collect::<Vec<usize>>();
    // println!("{:?}", resses);
    // println!("{:?}", vec);
    println!("{:?}", split);

}
